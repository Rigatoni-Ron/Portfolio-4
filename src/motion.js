// Spring shared by both directions of the card <-> modal morph.
// On open, the modal element runs it; on close, the card element runs it.
export const morph = { type: 'spring', stiffness: 380, damping: 40, mass: 1 }
